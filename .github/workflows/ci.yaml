name: ci-golang-workflow
on:
  push:
    branches:
      - main
      uses: actions/checkout@v2
      uses: actions/setup-go@v2
      with:
        go-version: 1.16
      run: go test
      run: go run math.sh